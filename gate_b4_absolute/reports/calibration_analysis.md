# Calibration analysis

Convention: `observed = intercept + slope * predicted`.
OOF linear calibration of NNLS ensembles evaluated; retained only if CV MAE improved (HIC CAL slightly worse than raw NNLS; TmApp CAL intermediate).
See `metrics/calibration_results.csv`.
